immich:
  ingress:
    hosts:
      - host: immich.localhost
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: immich-tls
        hosts:
          - immich.localhost

postgres:
  database: immich_staging

nfs:
  server: 192.168.18.96
  path: /mnt/cluster/nfs